ENVIRONMENT: rasa

COMANDI:

-) conda activate Rasa_es: attiva l'environment;
-) conda deactivate: disattiva l'environment;
-) rasa init: inizializza un chatbot;
-) rasa shell: inizializza la shell per interagire col chatbot;
-) rasa train: effettua un training (utile per riallenare il chatbot in seguito a delle modifiche);
-) rasa shell nlu: inizializza la shell relativa al NLU;
-) rasa run actions: inizializza il server per le azioni (DA ESEGUIRE SU UN SECONDO TERMINALE).

Per creare un bot su telegram è necessario cercare BotFather ed eseguire i seguenti passi:
-) /newbot
-) si fornisce il nome e lo username del chatbot;
-) viene restituita una chiave di autenticazione.

DA CONTROLLARE:
-) ngrok.exe http 5005: inizializza il collegamento a telegram (DA ESEGUIRE SU UN TERZO TERMINALE);
-) rasa run: avvia il server del chatbot.

RICEVUTO